#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

echo "==> Running oxlint --fix"
# lint is enforced by scripts/lint; don't let unfixable errors abort formatting
./node_modules/.bin/oxlint --fix || true

echo "==> Running oxfmt"
./node_modules/.bin/oxfmt
